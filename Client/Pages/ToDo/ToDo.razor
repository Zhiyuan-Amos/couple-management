@page "/todo"

@using Couple.Client.Pages.ToDo.Components

<ToDoTopBar IsDropDown="@IsDropdown"
            SelectedCategory="@SelectedCategory"
            OnClickCallback="@ToggleVisibility"
            OnSynchronisationCallback="@RefreshData" />
<ToDoListView ToDos="@ToDos" />
<BottomBar />
<AddIcon OnClickCallback="@AddToDo" />
<AnimatedCategoryListView @ref="CategoryListView"
                          OnClickCallback="@Select" />

