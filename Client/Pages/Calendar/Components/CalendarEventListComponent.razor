@implements IDisposable

<div id="events-list"
     style="@Style"
     class="w-100"
     @ontouchstart="@_verticalSwipeHandler.HandleTouchStart"
     @ontouchend="@_verticalSwipeHandler.HandleTouchEnd"
     @onkeypress:preventDefault="true">
    <Virtualize Items="@Events">
        <div class="px-3 py-2 border-bottom"
            @onclick="@(_ => EditEvent(context))">
            <h6 class="mb-1">
                @context.Title
            </h6>
            <div class="text-black-50">
                @context.Start.ToString("HH:mm") - @context.End.ToString("HH:mm")
            </div>
        </div>
    </Virtualize>
</div>
